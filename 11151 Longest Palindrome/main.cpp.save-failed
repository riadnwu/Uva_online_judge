#include <iostream>
#include<bits/stdc++.h>

using namespace std;

int main()
{
    int n;
    while(cin>>n )
    {
        for (int i=0;i<n;i++)
        {
            string s,s1,s2;
            cin>>s;
            s1=s;
            //for(int j=0;j<s.size()/2;j++)
            {
                s1=substr(0,(s.size()/2));
               reverse(s1.begin(),s1.end());
              cout<<s1;
            }
        }
    }
}
